# Xi Runtime 领域语言

Xi 将 Coding Agent 建模为可持久化的会话，使执行历史在保持因果关系的前提下可被
观察、续接、故障恢复、分叉和评测。

## 会话模型

**Session（会话）**:
一条连续的对话谱系，可以包含一轮或多轮由用户触发的执行。会话身份保持稳定，
其中每轮 Run 的身份各自独立。
_避免使用_: run、task 表示会话

**Run（运行轮次）**:
由一次用户任务触发的一轮执行。即使它延续已有会话的上下文，也拥有独立的身份和结果。
_避免使用_: session、step 表示运行轮次

**Event（事件）**:
Run 中已经发生的一条追加式事实，并通过因果关系连接到促成它的事件。Event 是观察和
重建 Session 的持久记录。
_避免使用_: 日志行、消息

**Trace（轨迹）**:
属于同一个 Session 的有序 Event 记录。一个 Trace 可以包含多个 Run，并保留事件之间
的因果关系。
_避免使用_: 对话文本、最终输出

**Resume（续接）**:
从正常完成的 Run 末端延续同一个 Session。新的 Run 继承已有对话语境与因果谱系。
_避免使用_: recovery、replay、rerun

**Recovery（故障恢复）**:
从失败或中断 Run 的安全历史点延续同一个 Session。它保留已完成的历史，但不把已经
发生的行动视为需要重做的指令。
_避免使用_: resume、replay、rerun

**Fork（分叉）**:
从历史成功 Run 的终点继承截至该点的对话语境，并创建独立的 Session 谱系。新谱系
保留来源关系，但身份与因果链不属于来源 Trace。
_避免使用_: resume、replay、rerun
